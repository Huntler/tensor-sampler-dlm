name: vst-plugin
channels:
  - pytorch
  - defaults
dependencies:
    - python=3.10
    - pyyaml
    - scikit-learn
    - scipy
    - matplotlib
    - tqdm
    - pip:
        - mido
        - tensorboard
        - torch
        - torchaudio