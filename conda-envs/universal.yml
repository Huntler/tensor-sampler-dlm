name: vst-plugin
 
dependencies:
    - python=3.9
    - scikit-learn
    - scipy
    - pandas
    - matplotlib
    - tqdm
    - mido
    - pip:
        - torch
        - tensorboard
        # downgrade because of pytorch bug
        - setuptools==59.5.0