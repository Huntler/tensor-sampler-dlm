name: vst-plugin
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
    - python=3.10
    - cudatoolkit=11.6
    - pyyaml
    - scikit-learn
    - scipy
    - matplotlib
    - tqdm
    - pip:
        - mido
        - tensorboard
        - torch
        - torchaudio